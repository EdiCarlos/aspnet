@model IEnumerable<TreinoMVC.Modelo.Livro>

@{
    ViewBag.Title = "Index";
}

<h2>Lista de Livros</h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Autor.AutorId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Autor.AutorId)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.LivroId }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.LivroId }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.LivroId })
        </td>
    </tr>
}

</table>
